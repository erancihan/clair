package pages

templ Games() {
	<div class="mt-16 sm:mt-16 sm:px-8">
		<div class="mx-auto max-w-7xl lg:px-8">
			<div class="relative px-4 sm:px-8 lg:px-12">
				<div class="mx-auto max-w-2xl lg:max-w-5xl">
					<div class="mb-8">
						<h1 class="text-4xl font-bold tracking-tight text-zinc-800 dark:text-zinc-100 sm:text-5xl">
							Games
						</h1>
						<p class="mt-4 text-base text-zinc-600 dark:text-zinc-400">
							Play against a random agent or challenge a friend via link.
						</p>
					</div>
				</div>

				<div class="mx-auto max-w-2xl lg:max-w-5xl">
					<div class="grid grid-cols-1 gap-4 sm:grid-cols-2">
						<a href="/games/tic-tac-toe" class="block">
							<div class="bg-white dark:bg-zinc-800 border border-zinc-200 dark:border-zinc-800 rounded-3xl p-8 shadow-sm">
								<h2 class="text-2xl font-bold tracking-tight text-zinc-800 dark:text-zinc-100 sm:text-3xl">
									Tic-Tac-Toe
								</h2>
								<p class="mt-4 text-base text-zinc-600 dark:text-zinc-400">
									Play against a random agent or challenge a friend via link.
								</p>
							</div>
						</a>
						<a href="/games/chess" class="block">
							<div class="bg-white dark:bg-zinc-800 border border-zinc-200 dark:border-zinc-800 rounded-3xl p-8 shadow-sm">
								<h2 class="text-2xl font-bold tracking-tight text-zinc-800 dark:text-zinc-100 sm:text-3xl">
									Chess 🛠️🚧
								</h2>
								<p class="mt-4 text-base text-zinc-600 dark:text-zinc-400">
									Challenge a friend via link.
								</p>
							</div>
						</a>
					</div>
				</div>
			</div>
		</div>
	</div>
}
